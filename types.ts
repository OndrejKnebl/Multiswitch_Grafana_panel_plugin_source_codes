export interface SimplePanelOptions {
    targetTTNServer: string;            // URL of the ttn server (network cluster) is stored here
    targetAPIkey: string;               // API key from TTS is stored here
    targetAppName: string;              // Application name is stored here
    targetEndDeviceName: string;        // End device name is stored here
}
